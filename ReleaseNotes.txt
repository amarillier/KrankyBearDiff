Release notes

0.2.NEXT
- Possible later: tray menu refresh for dynamic enable/disable; last folder for patch export; word-level or syntax features if scope grows.

Wednesday, April 15, 2026
0.2.3
- Context / Edit menus: omit “Delete line from …” when it matches “Remove line from left/right” on that row (same edit, one menu entry).
- Edit menu: copy left line, copy right line, row merge actions (same dynamic labels as the row context menu), delete left/right line — all use the selected aligned row; system tray unchanged.
- Edit → Copy unified patch (Shift+Cmd/Ctrl+U); File → Export unified patch… (Shift+Cmd/Ctrl+E); see Help → UNIFIED PATCH for patch(1) / Cygwin.
- Main window: left/right pane split ratio saved on quit and restored on launch (preference key splitOffset).

Wednesday, April 15, 2026
0.2.2
- Per-pane file reload: toolbar control (refresh icon) re-reads that side’s file from disk; disabled until a path is loaded; confirms before discarding unsaved edits on that side.
- Preferences — Sync scroll: matches the main toolbar and View → Sync scroll; the on/off state is stored and restored on launch like line numbers and whitespace.
- Preferences — Window: optional “Remember main window size between launches.” When enabled, the last canvas size is saved when you quit (menu, tray, or closing the main window) and restored on the next run; when disabled, the app opens at the default size (1100×800).
- Tooltips: further hardening of vendored fyne-tooltip (third_party) — quiet handling when the hover target has no canvas yet, no spurious logs when the tooltip layer is missing during teardown, and safer popup overlay setup.

Tuesday, April 14, 2026
0.2.1
- Update check: optional background check on launch (about once per 7 days) using vendored github.com/amarillier/go-update-checker; Help → Check for Updates unchanged.

Monday, April 14, 2026
0.2.0
- Update check: semver-style compare to GitHub latest; clearer update dialog; alternate artwork when local build is ahead of the latest release.
- Merge UX: clearer selected-row highlight; context menu and recent-files menu use *widget.PopUp with tooltip layers; vendored fyne-tooltip (third_party) falls back to the window layer for unknown overlays (e.g. system dialogs) so toolbar tooltips no longer log errors.
- Merge menu: row-specific labels (replace / insert / remove) and pane-aware ordering; same merge logic as before.
- Edit workflow: undo/redo (up to five merge steps), copy left/right/aligned row (context menu and Cmd/Ctrl+C), swap left/right files (toolbar, View menu, Shift+Cmd/Ctrl+X); sync-scroll control moved to the main toolbar as an icon.
- Find in pane: literal or regex (Go regexp), match case, prev/next (wraps), wide Find field; matches jump both panes to the aligned row; sync-scroll anchors refresh when enabled.
- Navigation shortcuts: Alt+, / Alt+. (prev/next change), Alt+Home / Alt+End (start/end of diff); View and Edit menu entries with shortcuts.
- Documentation: README and in-app Help updated for the above.

Monday, April 14, 2026
0.1.0
- initial checkin
- Windows, Linux and MacOS diff utility